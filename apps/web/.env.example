# URL of the ChatHR backend API (no trailing slash)
# For local development with docker compose:
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# NOTE: Production should use HTTPS and the real deployed API URL.
# JWT is stored in localStorage for MVP convenience.
# Production must switch to httpOnly cookies or a secure session mechanism.
